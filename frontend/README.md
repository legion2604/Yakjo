# Yakjo - Платформа для совместных поездок (Ride-sharing)

**Yakjo** — это современное веб-приложение для поиска попутчиков и организации совместных поездок, разработанное с акцентом на удобство, безопасность и мультиязычность.

---

## 🚀 Основные возможности

- 🔍 **Поиск поездок**: Удобный поиск по маршрутам, датам и количеству мест.
- 🚗 **Публикация поездок**: Возможность для водителей предлагать свои маршруты.
- 🌍 **Мультиязычность**: Полная поддержка трех языков: **Русский**, **English**, **Тоҷикӣ**.
- 🌓 **Темная тема**: Автоматическая и ручная поддержка светлой и темной тем оформления.
- 👤 **Личный кабинет**: Управление профилем, просмотр истории поездок и персональных данных.
- 🔐 **Безопасная авторизация**: Вход по номеру телефона через OTP (одноразовый код).
- 📝 **Гибкая регистрация**: Сбор необходимых данных (ФИО, дата рождения, авто) при первом входе.
- 📱 **Адаптивный дизайн**: Приложение оптимизировано для работы на смартфонах, планшетах и ПК.

---

## 🛠 Технологический стек

- **Frontend**: React 19 + Vite
- **Routing**: React Router 7
- **Icons**: Lucide React
- **API Client**: Fetch API (с поддержкой HttpOnly Cookie)
- **State Management**: React Context API
- **Styling**: Vanilla CSS (CSS Variables для тем)

---

## 📁 Структура проекта

```text
src/
├── api/            # Логика запросов к бекенду (apiClient, методы)
├── components/     # Переиспользуемые UI компоненты и Layout
│   ├── ui/         # Базовые атомарные элементы (Button, Input)
│   └── Layout/     # Шапка (Header) и другие структурные части
├── context/        # Глобальное состояние (Auth, Settings/Localization)
├── pages/          # Страницы приложения (Home, Search, Profile, etc.)
├── assets/         # Статические ресурсы (изображения, шрифты)
├── App.jsx         # Корневой компонент и маршрутизация
└── main.jsx        # Точка входа
```

---

## 🔌 Интеграция с Backend

Приложение настроено на работу с API по адресу: `https://api.yakjo.tj/v1`.

### Механизм авторизации:
Приложение использует **HttpOnly Cookies** для хранения сессии. Все запросы через `apiClient` автоматически включают `credentials: 'include'`.

### Обработка ошибок:
При недоступности бекенда или ошибках API, приложение переключается на **Mock Data** (демонстрационные данные), что позволяет проводить презентации и тестирование интерфейса без активного сервера.

> Подробная спецификация всех эндпоинтов находится в файле [API_DOCUMENTATION.md](./API_DOCUMENTATION.md).

---

## 🌍 Локализация

Для перевода используется `SettingsContext`. Весь статический текст вынесен в объект `translations`.
Для добавления нового текста:
1. Добавьте ключ в `translations` в `src/context/SettingsContext.jsx`.
2. Используйте хук `useSettings` и функцию `t('key.path')` в компоненте.

---

## 🎨 Стилизация и Темы

Проект использует CSS переменные (`:root` и `[data-theme='dark']`) в файле `src/index.css`.
Все компоненты используют эти переменные для цветов фона, текста и границ, что обеспечивает мгновенное переключение тем.

---

## 🛠 Как запустить проект

1. **Установка зависимостей**:
   ```bash
   npm install
   ```
2. **Запуск в режиме разработки**:
   ```bash
   npm run dev
   ```
3. **Сборка для продакшена**:
   ```bash
   npm run build
   ```

---

## 📄 Лицензия

Проект разработан специально для Yakjo. Все права защищены.
